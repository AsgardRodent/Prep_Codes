#include<iostream>
using namespace std;

int main()
{
    int a;
    cin>>a;
    if(a > 0)
    {
        cout<<"Number is +ve";
    }
    else if(a < 0)
    {
        cout<<"Number is -ve";
    }
    else
    {
        cout<<"Number is 0";
    }
    return 0;
}
